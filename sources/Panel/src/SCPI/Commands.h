#pragma once


namespace SCPI
{
    /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
    class BaseCommand
    {
    public:
        /// Создать и выполнить команду из буфера data. Возвращает false, елси это невозможно
        static bool CreateAndRun(uint8 *data, uint size);
    private:
        static bool Run(uint8 *data, uint size);
    };

    namespace Command
    {
        /////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
        class IDN : public BaseCommand
        {
        public:
            static bool Run(uint8 *data, uint size);
        };
    }
}
