#pragma once


///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class Vector
{
public:
	Vector(uint size = 0);
	~Vector();
	void *Data();
	uint Size();
private:
	/// Берёт создаёт массив для данных
	void Alloc(uint size);
	/// Освобождает ресурсы
	void Free();
	/// Размер буфера в байтах
	uint size;
	/// Буфер данных
	void *buffer;
};


///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class Packet
{
public:
	uint GetCRC();
	uint SizeData();
private:
};
