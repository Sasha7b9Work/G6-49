#pragma once
#include "Message.h"


/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
class Transceiver
{
    friend class Interface;
private:

    static void Transmit(Message *message);

    static bool Receive(Message *message);
};
