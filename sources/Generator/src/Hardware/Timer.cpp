#include "Timer.h"


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
void Timer::PauseOnTime(uint timeMS)
{
    uint time = HAL_GetTick();
    while (HAL_GetTick() - time < timeMS) { };
}
